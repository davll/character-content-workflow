# Plugin Development

- This repository is a Codex plugin. Primary skill development happens under `REPO_ROOT/skills/`.
- Do not develop directly under `.agents/skills/`. That directory may only be updated by running `npm run sync:agents-skills`.
- Read `README.md` before starting development.
